; YAFSML 配置模板
; 布尔值支持：true、yes、on 或 1。其他值均视为 false。

; 未使用 --launch-target 时，独立启动器选择的游戏。
; 可用值包括：eldenring、nightreign、sekiro、darksouls3 及其别名。
game=eldenring


; 各游戏的通用补丁
[patch]
; 跳过开场 Logo。
skip_intro=1

; 阻止原始、修改后或过大的 regulation.bin 数据写入存档。
prevent_regulation_save_write=1

; 使用基于 mimalloc 的 Dantelion 分配器。Nightreign 不支持该补丁。
patch_mem=1

; 为 Dantelion 分配器使用专用 mimalloc 堆。关闭时使用常规 mimalloc 分配函数。
patch_mem_dedicated_heap=0

; mimalloc 专用堆大小，单位为 MB。0 使用游戏对应的默认值。
patch_mem_heap_size=0

; 缓存解密后的 BHD 标头，减少归档启动时间。
boot_boost=1

; 存档文件名映射应用于当前选择的游戏。以点号开头时仅替换源文件扩展名；留空禁用对应映射。
replace_save_filename=
replace_seamless_coop_save_filename=

; 为需要 CJK 文本输入的模组保持 IME 启用。
enable_ime=0


; 通用优化
[tweak]
; 游戏进程的 CPU 亲和性策略。
;  0 = 默认（所有逻辑核）
;  1 = 除第一个逻辑核之外的所有逻辑核
;  2 = 所有能效核（Intel 第 12 代及更高版本）
;  3 = 所有性能核（Intel 第 12 代及更高版本）
;  4 = 除第一个逻辑核之外的所有性能核
; 警告: 不要在 Intel Ultra 系列 CPU 上对艾尔登法环 1.16.2 或以上版本使用策略 2/3/4
cpu_affinity=0


; 日志设置
[log]
; 显示调试控制台。
console=0

; 写入日志文件
log_file=0

; 最低日志级别：trace、debug、info、warn、error 或 off。
log_level=warn


; 游戏启动时加载的外部 DLL。
; 路径可以是相对于配置文件的路径，也可以是绝对路径。
; 未设置条件时，DLL 会在 SteamAPI_Init 之后加载。
; 可用条件：
;   - early
;   - delay,毫秒数
;   - after,名称。
[dll]
;seamlesscoop=SeamlessCoop/ersc.dll
;early_native=native1.dll|early
;delay_native=native2.dll|delay,5000
;after_native=native3.dll|after,delay_native


; 扫描并覆盖游戏数据的模组目录。
; 后声明的模组覆盖先声明的模组中的同名文件。
; 路径可以是相对路径或绝对路径。
[mod]
;mod1=mod
;mod2=C:\mymods\some_mod
